import { menuIsActive } from './events';
import { $navLinks, $navLogo, $navButton, $isActive } from './store';

export const modelHeader = {
    $navLinks,
    $navLogo,
    $navButton,
    $isActive,
    menuIsActive,
};
